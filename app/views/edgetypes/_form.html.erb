<%= form_for(@edgetype) do |f| %>
  <% if @edgetype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@edgetype.errors.count, "error") %> prohibited this edgetype from being saved:</h2>

      <ul>
      <% @edgetype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :desc %><br />
    <%= f.text_area :desc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
