        {
          "id": <%= node.id %>,
          "title": "<%= node.title %>",
          "label": "<%= node.title.length > 15 ? "#{node.title[0,12]}..." : node.title %>",
          "x": "<%= node.pos_x %>",
          "y": "<%= node.pos_y %>"
        }
