<%= form_for(@course_membership) do |f| %>
  <% if @course_membership.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course_membership.errors.count, "error") %> prohibited this course_membership from being saved:</h2>

      <ul>
      <% @course_membership.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course_id %><br />
    <%= f.number_field :course_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :role %><br />
    <%= f.number_field :role %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
