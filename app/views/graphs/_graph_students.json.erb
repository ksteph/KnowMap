        {
          <% student_records = Graph.find(params[:graph_id]).course.students.map{ |rec| [rec.last.capitalize, rec.first.capitalize, rec.id] } %>
          <% student.records.sort %>
          <% student_records.each do |rec| %>
          <%= rec[0] + ", " + rec[1] %> : <%= rec[2] %>
          <% end %>
        }